ru:
  activerecord:
    models:
      user:
        one: Пользователь
        other: Пользователи
    attributes:
      user:
        password: Пароль
    errors:
      models:
        user:
          attributes:
            email:
              blank: не может быть пустым
            password:
              blank: не может быть пустым